## 1. インポート確認フローの更新

- [x] 1.1 JSON インポート処理の実行前に、既存データ消失とバックエンド上書きを明示する確認ダイアログ文言を追加する
- [x] 1.2 キャンセル時はインポート処理を中断し、ローカル/バックエンドのデータを変更しない分岐を実装する
- [x] 1.3 承認時のみ既存の JSON 解析・検証後の反映処理へ進むようにフローを整理する

## 2. バックエンド全件上書き同期の実装

- [x] 2.1 ログイン済み時のみ実行される「全削除→インポートデータ再作成」の同期処理を task-sync 側に実装する
- [x] 2.2 親子関係を維持するため、親タスク先行作成と ID マッピングを使った再作成手順をインポート同期でも適用する
- [x] 2.3 上書き完了後に `GET /tasks` を再取得し、その結果で taskStore を確定する処理を実装する

## 3. 失敗時ハンドリングと通知

- [x] 3.1 バックエンド上書き失敗時にローカルのインポート結果は維持しつつ、同期失敗をユーザーへ通知する
- [x] 3.2 再同期/再試行の導線（再実行トリガーまたは明確な案内メッセージ）を追加する
- [x] 3.3 同期失敗時のログ出力を追加し、原因調査に必要な情報を記録する
