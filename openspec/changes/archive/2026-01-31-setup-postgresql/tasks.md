## 1. ローカル開発環境のデータベース構成

- [x] 1.1 docker-compose.yml に PostgreSQL サービス (`db`) を追加する（postgres:17 イメージ、環境変数で初期ユーザー・パスワード・データベース名を設定、名前付きボリュームでデータ永続化、ポート `5532:5432`）
- [x] 1.2 backend サービスに `depends_on: db` とデータベース接続用の環境変数 (`DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASSWORD`, `DB_NAME`) を追加する

## 2. データベース接続

- [x] 2.1 `jackc/pgx/v5` を Go の依存関係に追加する (`go get`)
- [x] 2.2 環境変数からデータベース接続情報を読み取り、`pgxpool` で接続プールを作成する処理を実装する（デフォルト値の適用を含む）
- [x] 2.3 `main.go` でアプリケーション起動時にデータベース接続を確立し、接続失敗時にエラー終了する処理を追加する
- [x] 2.4 `/health` エンドポイントを追加し、データベース接続の状態に応じて HTTP 200 または 503 を返すようにする

## 3. マイグレーション基盤

- [x] 3.1 `golang-migrate/migrate` を Go の依存関係に追加する (`go get`)
- [x] 3.2 `backend/migrations/` ディレクトリを作成し、`.gitkeep` を配置する
- [x] 3.3 `go:embed` を使ってマイグレーションファイルをバイナリに埋め込む処理を実装する
- [x] 3.4 アプリケーション起動時（データベース接続確立後、HTTP サーバー起動前）にマイグレーションを自動実行する処理を実装する（エラー時はプロセス終了）

## 4. 動作確認

- [x] 4.1 `docker compose up` で全サービスが起動し、バックエンドが PostgreSQL に接続できることを確認する
- [x] 4.2 `/health` エンドポイントが HTTP 200 を返すことを確認する
