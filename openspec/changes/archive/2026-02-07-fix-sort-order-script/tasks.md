## 1. プロジェクト構造

- [x] 1.1 `backend/cmd/fixsortorder/` ディレクトリを作成する
- [x] 1.2 `backend/cmd/fixsortorder/main.go` を作成する

## 2. データ構造の定義

- [x] 2.1 タスクの JSON 構造に対応する Go の struct を定義する
- [x] 2.2 JSON のキー名（camelCase）に対応する json タグを設定する

## 3. コア処理の実装

- [x] 3.1 標準入力から JSON を読み込む処理を実装する
- [x] 3.2 parentId ごとにタスクをグループ化する処理を実装する
- [x] 3.3 各グループ内で sortOrder 順に stable sort する処理を実装する
- [x] 3.4 ソート後に 0 から連番を振り直す処理を実装する
- [x] 3.5 修正後の JSON を標準出力に出力する処理を実装する（インデント付き）

## 4. 検証処理の実装

- [x] 4.1 sortOrder 以外のフィールドが変更されていないことを検証する処理を実装する
- [x] 4.2 検証失敗時にエラーメッセージを出力して exit code 1 で終了する

## 5. エラーハンドリング

- [x] 5.1 無効な JSON 入力時のエラーハンドリングを実装する
- [x] 5.2 配列以外の JSON 入力時のエラーハンドリングを実装する

## 6. 動作確認

- [x] 6.1 サンプルの JSON データで正常系の動作を確認する
- [x] 6.2 sortOrder が重複しているケースで動作を確認する
- [x] 6.3 空配列の入力で動作を確認する
