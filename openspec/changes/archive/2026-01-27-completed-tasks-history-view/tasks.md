## 1. ストア層：完了タスクのグループ化セレクタ

- [x] 1.1 `taskStore` に完了タスクを `completedAt` の日付（ローカルタイムゾーン）でグループ化するセレクタを追加する。戻り値は `{ date: string; taskIds: string[] }[]` 形式で日付降順。
- [x] 1.2 完了タスクの祖先タスク ID を収集するユーティリティ関数を作成する（ツリー表示用）。

## 2. 履歴ビューコンポーネント

- [x] 2.1 `CompletedTasksHistoryView` コンポーネントを作成する。日付セクションごとにタスクツリーを表示し、完了タスクが存在しない場合は空状態メッセージを表示する。
- [x] 2.2 日付セクションコンポーネントを作成する。日付ヘッダーと、その日の完了タスク＋祖先タスクによるツリーを表示する。
- [x] 2.3 `TaskTreeItem` に完了ハイライト（緑色）の表示をサポートする props を追加する。その日に完了したタスクのみ緑色で表示する。

## 3. ビュー切り替え

- [x] 3.1 `App.tsx` にタブ UI（一覧 / 履歴）を追加し、ビューの切り替え状態を管理する。
- [x] 3.2 選択されたタブに応じて `TaskTreeView` または `CompletedTasksHistoryView` を表示する。
