## 1. セッション永続化層

- [x] 1.1 `StoredSession` 型定義を追加（sessionId, username）
- [x] 1.2 localStorage へのセッション保存・読み込みユーティリティを作成
- [x] 1.3 `activeSessionId` の保存・読み込みユーティリティを作成

## 2. 認証ストア拡張

- [x] 2.1 authStore の状態に `sessions: StoredSession[]` を追加
- [x] 2.2 初期化時に localStorage からセッション一覧を復元するロジックを追加
- [x] 2.3 ログインアクションを拡張：成功時に localStorage にセッション追加、同一ユーザー再ログイン時は置換
- [x] 2.4 ログアウトアクションを拡張：localStorage から削除、他セッションへの自動切り替え
- [x] 2.5 `switchUser(sessionId)` アクションを追加：activeSessionId 更新、Cookie 書き換え、ストア更新
- [x] 2.6 初回ロード時の認証チェックを拡張：401時にセッション削除→次セッションで再試行

## 3. Cookie 操作

- [x] 3.1 JavaScript から Cookie を書き換えるユーティリティ関数を作成
- [x] 3.2 ユーザー切り替え時に Cookie `session` を更新する処理を実装

## 4. UI: ドロップダウンメニュー拡張

- [x] 4.1 ドロップダウンメニューに他のログイン済みユーザー一覧を表示
- [x] 4.2 ユーザー名クリックで `switchUser` を呼び出す処理を追加
- [x] 4.3 「別のアカウントでログイン」メニュー項目を追加
- [x] 4.4 「別のアカウントでログイン」クリックでログインダイアログを表示
- [x] 4.5 現在のアクティブユーザーにチェックマークを表示

## 5. エラーハンドリング

- [x] 5.1 API呼び出しで401が返った場合にセッションを削除する共通処理を追加
- [x] 5.2 全セッション期限切れ時に未ログイン状態へ遷移する処理を確認
